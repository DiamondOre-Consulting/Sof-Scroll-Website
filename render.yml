services:
  - type: static
    name: Sof-Scroll
    staticPublishPath: build
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
